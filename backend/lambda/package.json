{
  "name": "snapresume-backend",
  "version": "1.0.0",
  "description": "SnapResume Lambda backend API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npm install && zip -r lambda-function.zip . -x '*.git*' 'node_modules/.cache/*' 'test/*'"
  },
  "keywords": [
    "resume",
    "cv",
    "aws",
    "lambda"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {}
}
